---
phase: 01-清理-JDBC-和-CLI-模式
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - Cargo.toml
  - octopus-cli/src/main.rs
  - wiki/overview.md
  - CLAUDE.md
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-04
  - CLEANUP-05
must_haves:
  truths:
    - "octopus-jdbc crate directory does not exist"
    - "Cargo.toml workspace members do not contain octopus-jdbc"
    - "octopus-cli only supports interactive/repl mode"
    - "wiki/overview.md contains no JDBC references"
    - "CLAUDE.md contains no JDBC driver description"
  artifacts:
    - path: "octopus-jdbc/"
      provides: "Deleted JDBC driver crate"
      state: absent
    - path: "Cargo.toml"
      provides: "Workspace without octopus-jdbc member"
      contains: "members = [\"octopus-common\", \"octopus-executor\", \"octopus-cli\", \"octopus-worker\", \"octopus-coordinator\"]"
    - path: "octopus-cli/src/main.rs"
      provides: "CLI with only interactive/repl mode"
      removes:
        - "run_local function"
        - "run_batch function"
        - "mode = \"local\" argument"
    - path: "wiki/overview.md"
      provides: "Overview without JDBC references"
      removes:
        - "octopus-jdbc/ entry in directory structure"
        - "JDBC Type 4 driver reference"
    - path: "CLAUDE.md"
      provides: "CLAUDE.md without JDBC driver description"
      updates:
        - 'octopus-cli description from "Client CLI with local/interactive/batch modes" to "Client CLI (interactive/repl mode)"'
---

<objective>
Remove the octopus-jdbc module and clean up unnecessary modes in the CLI (keeping only interactive/repl mode). This is a cleanup task per the v1.1 milestone requirements.
</objective>

<context>
@Cargo.toml
@octopus-cli/src/main.rs
@wiki/overview.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete octopus-jdbc crate directory</name>
  <files>octopus-jdbc/</files>
  <read_first>
    - octopus-jdbc/Cargo.toml
    - octopus-jdbc/src/
  </read_first>
  <action>
    Delete the entire octopus-jdbc directory using `rm -rf`. This removes the JDBC Type 4 driver crate entirely.

    Verify the directory no longer exists after deletion.
  </action>
  <verify>
    <automated>test ! -d octopus-jdbc && echo "octopus-jdbc deleted successfully"</automated>
  </verify>
  <acceptance_criteria>
    - octopus-jdbc/ directory no longer exists
    - No remaining references to octopus-jdbc in the workspace
  </acceptance_criteria>
  <done>octopus-jdbc crate directory deleted from workspace</done>
</task>

<task type="auto">
  <name>Task 2: Update Cargo.toml workspace members</name>
  <files>Cargo.toml</files>
  <read_first>
    - /workspace/octopus/Cargo.toml
  </read_first>
  <action>
    Remove "octopus-jdbc" from the workspace members array in Cargo.toml.

    Change from:
    members = ["octopus-common", "octopus-executor", "octopus-cli", "octopus-worker", "octopus-coordinator", "octopus-jdbc"]

    To:
    members = ["octopus-common", "octopus-executor", "octopus-cli", "octopus-worker", "octopus-coordinator"]
  </action>
  <verify>
    <automated>grep -q 'octopus-jdbc' Cargo.toml && echo "FAIL: octopus-jdbc still in Cargo.toml" || echo "PASS: octopus-jdbc removed from workspace"</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml workspace members does not contain octopus-jdbc
    - All other workspace members remain unchanged
  </acceptance_criteria>
  <done>Cargo.toml updated, octopus-jdbc removed from workspace members</done>
</task>

<task type="auto">
  <name>Task 3: Clean octopus-cli to keep only interactive/repl mode</name>
  <files>octopus-cli/src/main.rs</files>
  <read_first>
    - /workspace/octopus/octopus-cli/src/main.rs
  </read_first>
  <action>
    Modify octopus-cli/src/main.rs to remove local and batch modes, keeping only interactive/repl mode:

    1. Remove the `mode` argument from the Cli struct (line with #[arg(long, default_value = "local")])
    2. Remove the `file` argument from the Cli struct (batch mode file input)
    3. Remove the `parquet`, `csv`, `json` arguments (local mode data sources) - OR keep these since they can be used with REPL
    4. Remove the `run_local` function entirely
    5. Remove the `run_batch` function entirely
    6. Modify `main()` to only call `run_repl(cli)?` - remove the mode match statement
    7. Update default coordinator address to not require --coordinator flag in REPL mode

    The REPL mode already exists and works correctly. Only remove the conditional logic for other modes.
  </action>
  <verify>
    <automated>grep -c "run_local\|run_batch\|mode.*local\|mode.*batch" octopus-cli/src/main.rs | xargs -I {} test {} -eq 0 && echo "PASS: local/batch mode code removed" || echo "FAIL: local/batch mode code still present"</automated>
  </verify>
  <acceptance_criteria>
    - run_local function does not exist
    - run_batch function does not exist
    - CLI only supports interactive/repl mode
    - `cargo build -p octopus-cli` succeeds
  </acceptance_criteria>
  <done>octopus-cli cleaned to keep only interactive/repl mode</done>
</task>

<task type="auto">
  <name>Task 4: Update wiki/overview.md to remove JDBC references</name>
  <files>wiki/overview.md</files>
  <read_first>
    - /workspace/octopus/wiki/overview.md
  </read_first>
  <action>
    Update wiki/overview.md to remove JDBC references:

    1. Remove "octopus-jdbc/          - JDBC Type 4 driver (pure Java via JNI)" from the Directory Structure section
    2. Remove any other JDBC-related content in the document

    The document should still contain all other directory structure entries and content, just without JDBC references.
  </action>
  <verify>
    <automated>grep -i "jdbc" wiki/overview.md && echo "FAIL: JDBC still referenced" || echo "PASS: No JDBC references in overview.md"</automated>
  </verify>
  <acceptance_criteria>
    - wiki/overview.md contains no JDBC references
    - Directory structure section still lists all other crates
  </acceptance_criteria>
  <done>wiki/overview.md updated, JDBC references removed</done>
</task>

<task type="auto">
  <name>Task 5: Update CLAUDE.md to remove JDBC driver description</name>
  <files>CLAUDE.md</files>
  <read_first>
    - /workspace/octopus/CLAUDE.md
  </read_first>
  <action>
    Update CLAUDE.md to remove JDBC driver references:

    1. Change 'octopus-cli — Client CLI with local/interactive/batch modes' to 'octopus-cli — Client CLI (interactive/repl mode)'

    No other changes needed in CLAUDE.md - the JDBC driver description is only present in the Crates section.
  </action>
  <verify>
    <automated>grep -i "jdbc" CLAUDE.md && echo "FAIL: JDBC still in CLAUDE.md" || echo "PASS: No JDBC in CLAUDE.md"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains no JDBC driver description
    - octopus-cli description updated to reflect interactive/repl only
  </acceptance_criteria>
  <done>CLAUDE.md updated, JDBC driver description removed</done>
</task>

</tasks>

<verification>
All cleanup tasks complete:
1. octopus-jdbc directory deleted
2. Cargo.toml workspace updated
3. octopus-cli cleaned to interactive/repl only
4. wiki/overview.md JDBC references removed
5. CLAUDE.md JDBC description removed

Run: cargo build --workspace to verify workspace builds without errors.
</verification>

<success_criteria>
- octopus-jdbc crate completely removed from workspace
- CLI only supports interactive/repl mode (no local/batch)
- No JDBC references remain in wiki/overview.md or CLAUDE.md
- cargo build --workspace succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-清理-JDBC-和-CLI-模式/01-01-SUMMARY.md`
</output>