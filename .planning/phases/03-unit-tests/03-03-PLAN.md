---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - octopus-executor/src/session.rs
  - octopus-executor/src/query.rs
  - octopus-coordinator/src/exchange_operator.rs
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02

must_haves:
  truths:
    - "ExecutorSession creates SessionContext and executes simple queries"
    - "ExecutorSession handles errors gracefully"
    - "ExchangeMode enum variants have correct partition counts"
    - "ExchangeOperator requires exactly one child"
  artifacts:
    - path: "octopus-executor/src/session.rs"
      contains: "#[cfg(test)] mod tests"
      provides: "EXEC-01 ExecutorSession unit tests"
    - path: "octopus-coordinator/src/exchange_operator.rs"
      contains: "#[cfg(test)] mod tests"
      provides: "EXEC-02 Exchange operator tests"
  key_links:
    - from: "session.rs"
      to: "SessionContext"
      via: "ExecutorSession::new()"
      pattern: "SessionContext::new"
---

<objective>
Unit tests for ExecutorSession (EXEC-01) and ExchangeOperator (EXEC-02).

**EXEC-01:** Test query execution via real DataFusion SessionContext, error handling.
**EXEC-02:** Per RESEARCH.md, ExchangeOperator is placeholder. Focus tests on ExchangeMode enum variants, partition_count(), to_stage_partitioning(), create_exchange_ticket(), and child validation.
</objective>

<context>
@octopus-executor/src/session.rs
@octopus-executor/src/query.rs
@octopus-coordinator/src/exchange_operator.rs
@octopus-common/src/test_utils/fixture.rs

**Locked decisions (per CONTEXT.md):**
- TEST-REAL-01: Real DataFusion + in-memory; no real files or external DBs
- TEST-FIXTURE-01: Minimal constants + TestRecordBatchFactory for variants
- TEST-MOCK-01: Mock workers/Flight only

**Note on EXEC-02:** Per 03-RESEARCH.md Pattern 4, actual sender/receiver backpressure tests are deferred until exchange transport is implemented.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExecutorSession tests (EXEC-01)</name>
  <files>octopus-executor/src/session.rs</files>
  <read_first>
    From octopus-executor/src/session.rs lines 1-49:
    ```rust
    pub struct ExecutorSession {
        context: SessionContext,
    }

    impl ExecutorSession {
        pub fn new() -> Result<Self> { ... }
        pub fn context(&self) -> &SessionContext { ... }
    }
    ```

    From octopus-executor/src/query.rs (if exists):
    - QueryExecutor struct and methods

    From TestRecordBatchFactory (fixture.rs):
    - `TestRecordBatchFactory::new()` — empty builder
    - `add_integer_column(name, values)` / `add_string_column(name, values)`
    - `build()` — creates RecordBatch
  </read_first>
  <action>
    Expand `#[cfg(test)] mod tests` in session.rs with these test cases:

    1. **test_session_new** — ExecutorSession::new() succeeds with valid config
    2. **test_session_context_access** — context() returns reference to inner SessionContext
    3. **test_session_execute_simple_query** — execute "SELECT 1 as id" returns correct result via DataFusion
    4. **test_session_execute_withAggregation** — execute "SELECT count(*) FROM (VALUES (1), (2), (3))" returns 3
    5. **test_session_execute_withJoin** — execute query with join syntax (if supported)
    6. **test_session_execute_invalid_sql** — invalid SQL returns error
    7. **test_session_with_test_record_batch** — use TestRecordBatchFactory to create batch, register as table, query it

    Use `async_with_timeout_or_panic` for tests that involve async operations.

    Note: SessionContext SQL execution returns DataFrame, call .collect() to get results.
    Pattern: `session.context().sql("SELECT ...").await?.collect().await?`
  </action>
  <verify>
    <automated>cargo test -p octopus-executor -- session::tests --nocapture</automated>
  </verify>
  <done>All 7 ExecutorSession test cases pass: query execution works, errors handled, TestRecordBatchFactory integration</done>
</task>

<task type="auto">
  <name>Task 2: ExchangeOperator tests (EXEC-02)</name>
  <files>octopus-coordinator/src/exchange_operator.rs</files>
  <read_first>
    From octopus-coordinator/src/exchange_operator.rs lines 18-64 and 184-199:

    ExchangeMode enum:
    ```rust
    pub enum ExchangeMode {
        Single,
        Hash { keys: Vec<String> },
        RoundRobin,
        Broadcast,
    }

    impl ExchangeMode {
        pub fn partition_count(&self) -> usize { ... }
        pub fn to_stage_partitioning(&self) -> StagePartitioning { ... }
    }
    ```

    StagePartitioning enum:
    ```rust
    pub enum StagePartitioning {
        Single,
        HashPartitioned { num_partitions: usize, keys: Vec<String> },
        RoundRobin { num_partitions: usize },
        Broadcast,
    }
    ```

    ExchangeOperator struct and methods:
    - `new(input: Arc<dyn ExecutionPlan>, mode: ExchangeMode) -> Self`
    - `exchange_id() -> &str`
    - `mode() -> &ExchangeMode`
    - `execute()` returns error (placeholder)
    - `with_new_children()` validates exactly 1 child

    Ticket creation:
    ```rust
    pub fn create_exchange_ticket(exchange_id: &str, partition: usize) -> String {
        format!("exchange:{}:{}", exchange_id, partition)
    }
    ```
  </read_first>
  <action>
    Add `#[cfg(test)] mod tests` to exchange_operator.rs with these test cases:

    1. **test_exchange_mode_single_partition_count** — Single mode returns partition_count() == 1
    2. **test_exchange_mode_hash_partition_count** — Hash mode returns partition_count() == 4
    3. **test_exchange_mode_roundrobin_partition_count** — RoundRobin mode returns partition_count() == 4
    4. **test_exchange_mode_broadcast_partition_count** — Broadcast mode returns partition_count() == 1
    5. **test_exchange_mode_to_stage_partitioning_single** — Single → StagePartitioning::Single
    6. **test_exchange_mode_to_stage_partitioning_hash** — Hash { keys: ["a"] } → HashPartitioned
    7. **test_exchange_mode_to_stage_partitioning_roundrobin** — RoundRobin → RoundRobin variant
    8. **test_exchange_mode_to_stage_partitioning_broadcast** — Broadcast → Broadcast variant
    9. **test_create_exchange_ticket_format** — create_exchange_ticket("abc", 1) returns "exchange:abc:1"
    10. **test_exchange_operator_with_new_children_valid** — with_new_children accepts exactly 1 child
    11. **test_exchange_operator_with_new_children_invalid** — with_new_children rejects 0 or 2+ children
    12. **test_exchange_operator_execute_returns_error** — execute() returns error indicating distributed required
    13. **test_exchange_operator_benefits_from_input** — benefits_from_input_partitioning returns [false]

    Note: ExchangeOperator requires an Arc<dyn ExecutionPlan> as input. Create a simple placeholder plan for testing:
    ```rust
    // Use EmptyExec as placeholder child
    let schema = Schema::new(vec![]);
    let empty_exec = Arc::new(EmptyExec::new(&schema));
    ```
  </action>
  <verify>
    <automated>cargo test -p octopus-coordinator -- exchange_operator::tests --nocapture</automated>
  </verify>
  <done>All 13 ExchangeOperator test cases pass: ExchangeMode variants, partition counts, ticket creation, child validation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session → DataFusion | SessionContext executes SQL, errors returned as OctopusError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | malformed SQL | mitigate | DataFusion returns error, caught and returned as OctopusError |
| T-03-08 | Information disclosure | query results | accept | Test environment, no sensitive data in fixtures |
| T-03-09 | Tampering | ExchangeOperator tests | accept | Tests verify algorithm invariants, not external state |

Test-only phase, no network or file system access.
</threat_model>

<verification>
cargo test -p octopus-coordinator -- exchange_operator::tests && cargo test -p octopus-executor -- session::tests
</verification>

<success_criteria>
- EXEC-01: 7 ExecutorSession test cases with real DataFusion execution
- EXEC-02: 13 ExchangeOperator test cases covering enum variants, partition counts, ticket creation, child validation
- All tests pass with `cargo test -p octopus-executor` and `cargo test -p octopus-coordinator`
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-tests/03-03-SUMMARY.md`
</output>