---
phase: 02-Test-Infrastructure
plan: '03'
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - octopus-common/src/test_utils/federated.rs
  - octopus-common/src/test_utils/mod.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "InMemoryConnectionPool is available for federated connector tests"
    - "MockTypeAdapter is available for type mapping tests"
    - "TestQueryContext with pre-configured SessionContext is available in coordinator"
  artifacts:
    - path: "octopus-common/src/test_utils/federated.rs"
      provides: "InMemoryConnectionPool and MockTypeAdapter"
      contains: "impl ConnectionPool"
    - path: "octopus-coordinator/src/test_utils.rs"
      provides: "TestQueryContext"
      contains: "impl TestQueryContext"
  key_links:
    - from: "octopus-executor"
      to: "octopus-common::test_utils::federated"
      via: "InMemoryConnectionPool import"
      pattern: "use octopus_common::test_utils::federated"
---

<objective>
Create InMemoryConnectionPool for connector tests and TestQueryContext for coordinator query tests, completing the Phase 2 success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@octopus-common/src/federated.rs
@octopus-coordinator/src/query_service.rs
@octopus-common/src/test_utils/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InMemoryConnectionPool and MockTypeAdapter</name>
  <files>
    - octopus-common/src/test_utils/federated.rs
    - octopus-common/src/test_utils/mod.rs
  </files>
  <read_first>
    - octopus-common/src/federated.rs (TypeAdapter, ConnectionPool, PoolStats traits)
    - octopus-common/src/test_utils/mod.rs (current state)
  </read_first>
  <action>
Create octopus-common/src/test_utils/federated.rs with:

**InMemoryConnectionPool** - a fake ConnectionPool implementation per ARCHITECTURE.md "Pattern 1: Trait-Based Mocking":
- struct InMemoryConnectionPool with configurable behavior
- Fields: Arc<RwLock<Vec<Box<dyn Any + Send>>>> (pool of fake connections), PoolStats
- `new(max_connections: usize)` - create with max pool size
- `with_connections(n: usize)` - pre-populate with n fake connections
- impl ConnectionPool for InMemoryConnectionPool:
  - `get()` - returns a fake connection or PoolError if exhausted
  - `release(conn)` - puts connection back in pool
  - `stats()` - returns current PoolStats
- Implement proper PoolStats tracking

**MockTypeAdapter** - a fake TypeAdapter for testing:
- struct MockTypeAdapter(DatabaseType)
- `new(db_type: DatabaseType)` - create with specific database type
- impl TypeAdapter for MockTypeAdapter:
  - `database_type()` - returns the configured type
  - `to_arrow_type()` - returns DataType::Utf8 for varchar/text types, Int64 for integer types, Float64 for numeric types (simple mapping)
  - `from_arrow_type()` - reverse mapping
- Add a simple schema builder method: `mock_schema(table_name: &str, columns: Vec<(&str, &str)>) -> Schema`

Update octopus-common/src/test_utils/mod.rs to add:
```rust
pub mod federated;
pub use federated::{InMemoryConnectionPool, MockTypeAdapter};
```

Per D-12, these will be used by executor and coordinator tests via the test-utils feature.
  </action>
  <acceptance_criteria>
    - grep -c 'InMemoryConnectionPool' octopus-common/src/test_utils/federated.rs returns 1
    - grep -c 'MockTypeAdapter' octopus-common/src/test_utils/federated.rs returns 1
    - grep -c 'impl ConnectionPool for InMemoryConnectionPool' octopus-common/src/test_utils/federated.rs returns 1
    - grep -c 'pub mod federated' octopus-common/src/test_utils/mod.rs returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'InMemoryConnectionPool' /workspace/octopus/octopus-common/src/test_utils/federated.rs && grep -c 'pub mod federated' /workspace/octopus/octopus-common/src/test_utils/mod.rs</automated>
  </verify>
  <done>InMemoryConnectionPool and MockTypeAdapter are available in test_utils</done>
</task>

<task type="auto">
  <name>Task 2: Create TestQueryContext for coordinator</name>
  <files>octopus-coordinator/src/test_utils.rs</files>
  <read_first>
    - octopus-coordinator/src/query_service.rs (SessionContext usage)
    - octopus-coordinator/Cargo.toml (dependencies)
  </read_first>
  <action>
Create octopus-coordinator/src/test_utils.rs with TestQueryContext:

**TestQueryContext** - pre-configured SessionContext for query testing:
- struct TestQueryContext with SessionContext and UdfRegistryImpl
- `new()` - create with fresh SessionContext
- `with_udf(name: &str, udf: ScalarUDF)` - register a UDF before returning
- `context(&self) -> &SessionContext` - access the inner context
- `udf_registry(&self) -> &UdfRegistryImpl` - access the UDF registry

This addresses the integration point in query_service.rs line 93-96 where `self.context.sql(sql)` is called. Tests need a pre-configured context with known schemas and UDFs.

The SessionContext is created via `SessionContext::new()` per PITFALLS.md Pitfall 2 (fresh state per test).

Example usage in tests:
```rust
let ctx = TestQueryContext::new();
ctx.with_udf("to_upper", create_to_upper_udf());
let df = ctx.context().sql("SELECT to_upper(name) FROM users").await?;
```
  </action>
  <acceptance_criteria>
    - grep -c 'TestQueryContext' octopus-coordinator/src/test_utils.rs returns 1
    - grep -c 'impl TestQueryContext' octopus-coordinator/src/test_utils.rs returns 1
    - grep -c 'SessionContext' octopus-coordinator/src/test_utils.rs returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'TestQueryContext' /workspace/octopus/octopus-coordinator/src/test_utils.rs && grep -c 'SessionContext' /workspace/octopus/octopus-coordinator/src/test_utils.rs</automated>
  </verify>
  <done>TestQueryContext available for coordinator tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production traits | Mock implementations for testing only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | InMemoryConnectionPool | accept | Only used in test code, not production |
| T-02-05 | Tamper | TestQueryContext | accept | Test-only code, isolated from production |
</threat_model>

<verification>
```bash
cd /workspace/octopus && cargo check -p octopus-coordinator --features test-utils
```
</verification>

<success_criteria>
Coordinator compiles with test-utils feature, TestQueryContext is accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Test-Infrastructure/02-03-SUMMARY.md`
</output>