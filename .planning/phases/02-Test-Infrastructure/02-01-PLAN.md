---
phase: 02-Test-Infrastructure
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - octopus-common/Cargo.toml
  - octopus-coordinator/Cargo.toml
  - octopus-executor/Cargo.toml
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Dev-dependencies (mockall 0.13, tempfile, tokio-test) are available workspace-wide"
    - "octopus-common has test-utils feature gate"
    - "Feature is activated in coordinator and executor dev-dependencies"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace dev-dependencies"
      contains: "mockall"
    - path: "octopus-common/Cargo.toml"
      provides: "test-utils feature gate"
      contains: "features.*test-utils"
    - path: "octopus-coordinator/Cargo.toml"
      provides: "Dev deps with test-utils feature"
      contains: "octopus-common.*test-utils"
    - path: "octopus-executor/Cargo.toml"
      provides: "Dev deps with test-utils feature"
      contains: "octopus-common.*test-utils"
  key_links:
    - from: "octopus-coordinator/Cargo.toml"
      to: "octopus-common"
      via: "dev-dependency with test-utils feature"
      pattern: "octopus-common.*features.*test-utils"
    - from: "octopus-executor/Cargo.toml"
      to: "octopus-common"
      via: "dev-dependency with test-utils feature"
      pattern: "octopus-common.*features.*test-utils"
---

<objective>
Configure workspace-wide test dependencies and the test-utils feature gate in octopus-common. This enables Phase 2-5 test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Cargo.toml
@octopus-common/Cargo.toml
@octopus-coordinator/Cargo.toml
@octopus-executor/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace dev-dependencies</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace root, current dependencies section)
  </read_first>
  <action>
Add mockall = "0.13" to [workspace.dependencies] section.

Current workspace.dependencies does not have mockall, tempfile, or portpicker. Add them:

```toml
mockall = "0.13"
tempfile = "3"
portpicker = "0.1"
tower-test = "0.4"
```

These are needed for trait mocking (mockall), RAII temp files (tempfile), ephemeral ports (portpicker), and service testing (tower-test).
  </action>
  <acceptance_criteria>
    - grep -c '^mockall = "0.13"' Cargo.toml returns 1
    - grep -c '^tempfile = "3"' Cargo.toml returns 1
    - grep -c '^portpicker = "0.1"' Cargo.toml returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^mockall = "0.13"' /workspace/octopus/Cargo.toml && grep -c '^tempfile = "3"' /workspace/octopus/Cargo.toml && grep -c '^portpicker = "0.1"' /workspace/octopus/Cargo.toml</automated>
  </verify>
  <done>Workspace dependencies include mockall 0.13, tempfile 3, portpicker 0.1, tower-test 0.4</done>
</task>

<task type="auto">
  <name>Task 2: Add test-utils feature gate to octopus-common</name>
  <files>octopus-common/Cargo.toml</files>
  <read_first>
    - octopus-common/Cargo.toml (current content)
  </read_first>
  <action>
Add a [features] section to octopus-common/Cargo.toml with a test-utils feature that enables mockall and tempfile dev-dependencies:

```toml
[features]
test-utils = ["dep:mockall", "dep:tempfile"]
```

Then add dev-dependencies using the feature:

```toml
[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread"] }
mockall = { workspace = true, optional = true }
tempfile = { workspace = true, optional = true }
```

The features.test-utils flag controls whether test utilities are compiled. Per D-13, this avoids shipping test code in release builds.
  </action>
  <acceptance_criteria>
    - grep -c '^\[features\]' octopus-common/Cargo.toml returns 1
    - grep -c 'test-utils' octopus-common/Cargo.toml returns 2 (definition + usage)
    - grep -c '^mockall = { workspace = true, optional = true }' octopus-common/Cargo.toml returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^\[features\]' /workspace/octopus/octopus-common/Cargo.toml && grep -c 'test-utils' /workspace/octopus/octopus-common/Cargo.toml && grep -c 'mockall.*optional' /workspace/octopus/octopus-common/Cargo.toml</automated>
  </verify>
  <done>octopus-common has test-utils feature gate with mockall and tempfile as optional deps</done>
</task>

<task type="auto">
  <name>Task 3: Enable test-utils in dependent crates dev-dependencies</name>
  <files>octopus-coordinator/Cargo.toml, octopus-executor/Cargo.toml</files>
  <read_first>
    - octopus-coordinator/Cargo.toml
    - octopus-executor/Cargo.toml
  </read_first>
  <action>
Update octopus-coordinator/Cargo.toml [dev-dependencies] section to include octopus-common with test-utils feature:

```toml
octopus-common = { path = "../octopus-common", features = ["test-utils"] }
```

Add tower-test and portpicker dev-dependencies:

```toml
tower-test = "0.4"
portpicker = "0.1"
```

Update octopus-executor/Cargo.toml [dev-dependencies] section similarly:

```toml
octopus-common = { path = "../octopus-common", features = ["test-utils"] }
mockall = "0.13"
tempfile = "3"
tokio-test = "0.4"
```

Per D-12, each crate adds octopus-common with test-utils feature as dev-dependency.
  </action>
  <acceptance_criteria>
    - grep -c 'features = \["test-utils"\]' octopus-coordinator/Cargo.toml returns 1
    - grep -c 'features = \["test-utils"\]' octopus-executor/Cargo.toml returns 1
    - grep -c '^tower-test' octopus-coordinator/Cargo.toml returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'features = \["test-utils"\]' /workspace/octopus/octopus-coordinator/Cargo.toml /workspace/octopus/octopus-executor/Cargo.toml && grep -c '^tower-test' /workspace/octopus/octopus-coordinator/Cargo.toml</automated>
  </verify>
  <done>coordinator and executor have octopus-common with test-utils feature in dev-dependencies</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A - Configuration-only change, no runtime trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tamper | Cargo.toml | mitigate | Lock file pins exact versions, git commit tracking |
</threat_model>

<verification>
```bash
cargo check --workspace --all-targets
```
</verification>

<success_criteria>
Workspace builds with new dev-dependencies, test-utils feature gate is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Test-Infrastructure/02-01-SUMMARY.md`
</output>